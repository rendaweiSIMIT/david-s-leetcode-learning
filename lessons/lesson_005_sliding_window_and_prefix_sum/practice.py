"""Lesson 005 exercises: sliding windows and prefix sums."""


def max_sum_fixed_window(numbers, k):
    """Return the largest sum among all contiguous windows of length k.

    Assume 1 <= k <= len(numbers).
    """
    # Example: max_sum_fixed_window([2, 1, 5, 1, 3, 2], 3) == 9
    raise NotImplementedError


def range_sum_queries(numbers, queries):
    """Return each inclusive [left, right] range sum in query order.

    Every query contains valid indices.
    """
    # Example: range_sum_queries([2, 4, 1, 3], [(0, 2), (2, 2)]) == [7, 1]
    raise NotImplementedError


def min_subarray_len(target, numbers):
    """Return the minimum length with sum >= target, or 0 if none exists.

    All values in numbers are positive integers and target is positive.
    """
    # Example: min_subarray_len(7, [2, 3, 1, 2, 4, 3]) == 2
    raise NotImplementedError

