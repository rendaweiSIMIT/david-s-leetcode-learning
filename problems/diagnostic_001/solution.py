"""Session 001 baseline diagnostic.

Complete this file without AI or search. Keep failed approaches as comments until
the review is finished; they are useful diagnostic evidence.
"""


def two_sum_indices(nums: list[int], target: int) -> list[int]:
    """Return indices of the unique pair whose values sum to target.

    Approach:
    Complexity:
    """
    raise NotImplementedError


def is_valid_brackets(s: str) -> bool:
    """Return whether a string of (), [], and {} is correctly balanced.

    Invariant:
    Complexity:
    """
    raise NotImplementedError


def lower_bound(nums: list[int], target: int) -> int:
    """Return the first index i with nums[i] >= target, or len(nums).

    Search-interval invariant:
    Complexity:
    """
    raise NotImplementedError

