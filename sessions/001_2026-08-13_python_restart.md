# Session 001：Python 零起点重启

日期：2026-08-13  
计划用时：60–90 分钟  
目标：重新建立“打开文件—阅读代码—运行—修改—再次运行”的最小闭环。

## 起点修正

学习者上一次持续写代码约在七年前，目前连基础写法也已遗忘。因此本次不进行算法诊断，不假定还记得变量、循环、函数或容器语法。

这不代表从未学过。已有知识可能会在动手后快速恢复，但在实际表现出现前不跳级。

## 本次内容

- 如何在 PowerShell 中运行 `.py` 文件
- `print` 输出
- 变量与整数、字符串、布尔值
- 列表及下标
- `if` 判断
- `for` 循环与缩进
- 用 `def` 定义最简单的函数

## 操作顺序

1. 阅读 `lessons/001_python_restart/README.md`。
2. 运行 `examples.py`，观察输出。
3. 每次只修改一处，再次运行，观察“代码变化 → 输出变化”。
4. 完成 `practice.py` 中标有 `TODO` 的三个小任务。
5. 不追求速度；遇到报错时保留完整报错信息。

## 完成记录

- 实际用时：
- 是否成功运行示例：是
- 已完成：按说明修改名字、列表、判断阈值和 `double` 的调用参数，修改后运行正确
- 完成的 TODO：三个均已完成；姓名变量、列表元素和 `triple` 返回值均修改正确
- 遇到的第一个报错：
- 目前最陌生的语法：
- 验证结果：程序正常输出列表中的六个数字，`triple(7)` 返回 21
- 口头检查：能准确说明循环变量逐个经过列表元素，循环体执行六次并在列表结束后停止
- 节奏反馈：学习者认为当前拆分过细，希望适度加速；后续将相关基础语法合并训练
- 环境备注：源文件为有效 UTF-8；自动化输出抓取曾因 PowerShell 使用 GBK (`cp936`) 显示乱码，代码和源文件均无问题
- 下一步：进入 Lesson 002，一次恢复函数、判断、列表、字典和集合
