# Session 003：数组与哈希入门

日期：2026-08-19  
计划用时：60–90 分钟  
目标：恢复下标遍历，并第一次独立用集合/字典优化查找。

## 练习

见 `lessons/lesson_003_arrays_and_hashing/practice.py`：

1. `index_of_first`：同时遍历下标和值。
2. `contains_duplicate`：利用集合判断是否见过。
3. `two_sum_indices`：用一次遍历找到和为目标值的两个下标。

## 规则

- 可以阅读本节 README 的语法和复杂度表。
- 先独立完成前两题，再尝试第三题。
- 第三题卡住时保留代码并请求一级提示，不直接搜索完整答案。
- 每题先保证正确，再说明时间与额外空间复杂度。

## 测试命令

```powershell
python -m unittest lessons.lesson_003_arrays_and_hashing.test_practice -v
```

## 完成记录

- 实际用时：
- 完成日期：2026-08-20
- 首次完整测试：Two Sum 暴力解完成后 8/8 通过
- 最终测试结果：字典优化版 8/8 通过
- 使用的提示级别：详细概念讲解与代码骨架
- 第一题问题：最初找到目标后继续遍历，返回了最后一个匹配下标；改为匹配后立即返回
- 第二题：集合查重独立完成并通过
- 第三题第一版：两层循环向后查找，正确，时间 `O(n²)`、额外空间 `O(1)`
- 第三题优化版：用字典保存“数字 → 下标”，单次遍历，平均时间 `O(n)`、额外空间 `O(n)`
- 优化版首次错误：判断 `needed in seen` 后读取了 `seen[number]`；除 `number == needed` 的重复值案例外会触发 `KeyError`
- 最终修正：读取 `seen[needed]`；理解集合只保存成员，字典保存键到附加信息的映射
- 后续问题：为什么集合和字典检索不需要显式 `for`；由此进一步学习哈希函数、槽位定位、冲突与平均 `O(1)`
- 底层追问：哈希值如何定位槽位；理解了“哈希值经取模/掩码得到初始数组下标，冲突后开放寻址继续探测”的简化模型，并区分算法原理与 CPython 探测公式的实现细节
- 下一步：D+1 无提示复现字典版 Two Sum；进入双指针、滑动窗口与前缀和
